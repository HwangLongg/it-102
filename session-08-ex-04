#include <stdio.h>
int main (){
    int max ;
    int array[2][3] = {{1,2,3},{4,5,6}};
    max = array[0][0];
    for (int i = 0 ; i < 2 ; i++)
    {
        for (int j = 0 ; j < 3 ; j++){
            if (array[i][j] > max ){
                max = array[i][j];
            }
        }
    }
    printf ("%d",max);
    return 0;
}